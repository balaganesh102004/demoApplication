# Copy this file to .env and fill in your values for local postgres dev.
# Leave DATABASE_URL blank to use the default JSON flat-file backend.

# Neon postgres connection string (only needed if you want postgres locally)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Express API port (default: 3001)
# PORT=3001
